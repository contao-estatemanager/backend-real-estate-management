services:
    _defaults:
        autowire: true
        autoconfigure: true
        public: true

    ContaoEstateManager\BackendRealEstateManagement\:
        resource: ../../
        exclude: ../../{ContaoManager,Model,Resources}
